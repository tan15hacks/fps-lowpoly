const config = {
  appId: 'com.tan15hacks.polygonoutpost',
  appName: 'Polygon Outpost',
  webDir: 'dist',
  server: { androidScheme: 'https' },
  android: { allowMixedContent: false, backgroundColor: '#111821' },
};

export default config;
